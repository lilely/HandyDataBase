
PROJECT(handy_database)

INCLUDE_DIRECTORIES(
include
)

AUX_SOURCE_DIRECTORY(src DIR_SRCS)

SET(DATABASE_SRC
${DIR_SRCS}
)

ADD_EXECUTABLE(${PROJECT_NAME} ${DATABASE_SRC})

#TARGET_LINK_LIBRARIES(${PROJECT_NAME} m)